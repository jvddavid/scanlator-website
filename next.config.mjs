/** @type {import('next').NextConfig} */
const nextConfig = {
  swcMinify: true,
  compress: true,
  poweredByHeader: false,
  reactStrictMode: true,
  cleanDistDir: true,
  optimizeFonts: true,
}

export default nextConfig
